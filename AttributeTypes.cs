using System;
using Workflows.Handler.Attributes;

namespace Workflows.Handler.Constants
{
    public static class AttributeTypes
    {
        public static readonly Type EmitSignal = typeof(EmitSignalAttribute);
    }
}